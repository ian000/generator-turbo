{
  "name": "<%= slugify(projectName) %>",
  "version": "<%= version %>",
  "repository":{
    "type": "<%= repositoryType %>",
    "url":"<%= repositoryUrl%>"
  },
  "description": "<%= description %>",
  "main": "<%= main %>",
  "author": {
    "name": "<%= authorName %>",
    "email":"<%= authorEmail %>",
    "url": "<%= authorUrl %>"
  },
  "license": "MIT",
  "dependencies": {
    "gulp-turbo": "^1.0.17"
  },
  "devDependencies": {
    "gulp": "^3.9.0"
  }

}
